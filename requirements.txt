# Core Neuraxon (neuraxon.py, neuraxon2.py, MultiNeuraxon2.py) uses only
# the Python standard library — no dependencies required.

# The Image Evolution Engine (image_evolution/ + app.py) adds:
pillow>=10.0
numpy>=1.24
gradio>=4.0
